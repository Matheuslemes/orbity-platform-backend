server:
  port: 8082

spring:
  application:
    name: ms-media

  # MongoDB
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/mediaOrbity}
      auto-index-creation: true
      uuid-representation: standard

    # Redis (cache e utilidades)
    # EN: Redis (cache and utilities)
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

  #  Multipart (upload limites)
  # EN: Multipart (upload limits)
  servlet:
    multipart:
      max-file-size: ${MEDIA_MAX_FILE_SIZE:20MB}
      max-request-size: ${MEDIA_MAX_REQUEST_SIZE:20MB}

  # Cache (Redis)
  # EN: Cache (Redis)
  cache:
    type: redis
    redis:
      time-to-live: 5m
      cache-null-values: false

  # Kafka
  # EN: Kafka
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:127.0.0.1:9094}
    admin:
      auto-create: true
    consumer:
      group-id: ${KAFKA_GROUP_ID:ms-media}
      enable-auto-commit: false
      # properties:
      # EN: properties:
      #   spring.json.trusted.packages: "*"
      #   EN: spring.json.trusted.packages: "*"
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

# mapas de tópicos (mantém prefixo 'orbity.kafka.*' como no projeto)
# EN: topic maps (keeps the 'orbity.kafka.*' prefix as in the project)
orbity:
  kafka:
    producer:
      topic-key: media-events
      topics:
        media-events:
          name: ${MEDIA_EVENTS_TOPIC:media.asset.events.v1}
          partitions: 1
          replicas: 1
    # (opcional) consumidores deste serviço
    # EN: (optional) consumers of this service
    consumer:
      topic-key: media-commands
      topics:
        media-commands:
          name: ${MEDIA_COMMANDS_TOPIC:media.commands.v1}
          concurrency: ${MEDIA_COMMANDS_CONCURRENCY:1}

# suporte ao TopicAdminConfig (auto-criar tópicos via propriedade simples)
# EN: support for TopicAdminConfig (auto-create topics via simple property)
app:
  kafka:
    topics:
      auto-create: ${TOPIC_AUTO_CREATE:true}
      media-events: ${MEDIA_EVENTS_TOPIC:media.asset.events.v1}
      media-commands: ${MEDIA_COMMANDS_TOPIC:media.commands.v1}

# propriedades específicas do ms-media
# EN: ms-media specific properties
media:
  storage:
    # local | minio | azure
    # EN: local | minio | azure
    type: ${MEDIA_STORAGE_TYPE:local}

    local:
      base-path: ${MEDIA_BASE_PATH:./.media}
      public-base-url: ${MEDIA_PUBLIC_BASE_URL:http://localhost:8083/files}

    minio:
      endpoint: ${MINIO_ENDPOINT:http://localhost:9000}
      bucket: ${MINIO_BUCKET:media}
      access-key: ${MINIO_ACCESS_KEY:minioadmin}
      secret-key: ${MINIO_SECRET_KEY:minioadmin}
      region: ${MINIO_REGION:us-east-1}
      secure: ${MINIO_SECURE:false}

    azure:
      # Config para Azure Blob / Azurite
      # EN: Azure Blob / Azurite config
      # Connection String: em prod pode vir de Key Vault / env;
      # em dev, por padrão usa o Azurite local em http://localhost:10000
      connection-string: "${AZURE_STORAGE_CONNECTION_STRING:DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFeqCnf2cZxGZQZg+5h2E9QZxZJZ0I=;BlobEndpoint=http://localhost:10000/devstoreaccount1;}"
      account-name: ${AZURE_ACCOUNT_NAME:devstoreaccount1}
      container: ${AZURE_BLOB_CONTAINER:media}
      # Azurite NÃO suporta User Delegation SAS → mantenha false em dev
      # EN: Azurite does NOT support User Delegation SAS → keep false in dev
      use-user-delegation-sas: ${AZURE_USE_USER_DELEGATION_SAS:false}
      # TTL padrão das URLs SAS geradas pelo ms-media (ex.: 300s = 5 minutos)
      # EN: Default TTL for SAS URLs generated by ms-media
      sas-default-ttl: ${AZURE_SAS_DEFAULT_TTL:300s}

  cache:
    # TTL curto para metadados/URLs pré-assinadas
    # EN: short TTL for metadata/pre-signed URLs
    metadata-ttl: ${MEDIA_METADATA_TTL:300s}
    url-ttl: ${MEDIA_URL_TTL:300s}

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha
    tryItOutEnabled: true

logging:
  level:
    org.springframework.kafka: info
    org.springframework.data.redis: info
    org.springframework.data.mongodb: info
    br.com.orbity.media: info

# PROFILE: secured → habilita OAuth2 Resource Server (Keycloak)
# EN: PROFILE: secured → enables OAuth2 Resource Server (Keycloak)
# Ative com: -Dspring.profiles.active=secured
# EN: Enable with: -Dspring.profiles.active=secured
---
spring:
  config:
    activate:
      on-profile: secured
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${OAUTH2_ISSUER_URI:http://localhost:8085/realms/master}
          # jwk-set-uri: ${OAUTH2_JWKS_URI:http://localhost:8085/realms/master/protocol/openid-connect/certs}
          # EN: jwk-set-uri: ${OAUTH2_JWKS_URI:http://localhost:8085/realms/master/protocol/openid-connect/certs}
